# 輸出コンプライアンスに関する判断の記録

最終更新: 2026-09-02

`app.json` の `ios.infoPlist.ITSAppUsesNonExemptEncryption` を `false`（＝適用除外に当たらない
暗号は使用していない）として申告している。その判断の根拠を残す。

> **これは法律意見ではない。** 貿易コンプライアンスの専門家によるレビューは受けていない。
> 下記は開発者本人が事実関係を整理したものであり、疑義が生じた場合は専門家に確認すること。

## このアプリが使っている暗号

| 用途 | 実装 | 備考 |
|---|---|---|
| ローカルDBの保存時暗号化 | SQLCipher (AES-256)。`app.json` の `expo-sqlite` プラグインで `useSQLCipher: true` | 端末内の `fx_journal_v2.db` のみが対象 |
| 上記の鍵の保管 | iOS Keychain / Android Keystore（`expo-secure-store` 経由） | OS が提供する仕組み |
| 通信 | HTTPS のみ（Sentry / RevenueCat / EAS Update） | OS のネットワークスタックを使用 |

## このアプリがしていないこと

- 暗号化・復号・鍵管理を**ユーザーに機能として提供していない**。設定画面にも暗号に関する項目は無い。
- VPN・トンネリング・メッセージングの暗号化・ファイル暗号化ツールのいずれでもない。
- 独自の暗号アルゴリズムを実装していない（SQLCipher が用いる AES は公開された標準規格）。
- 暗号機能を売り文句や販売対象としていない。ストア説明文でも「端末内に暗号化して保存する」という
  データ保護の説明に留めている。
- ユーザーが入力したデータを外部サーバーへ送信していない（バックアップの保存先はユーザーが選ぶ）。

## 適用除外と判断した根拠

米国 EAR の Category 5 Part 2 **注記4（ancillary cryptography、付随的暗号）** に該当すると判断した。
注記4は、品目の**主たる機能が情報セキュリティではなく**、暗号がその主たる機能を支えるためだけに
限定して使われている場合、Category 5 Part 2 の規制対象から外すというもの。

本アプリの主たる機能はFXトレードの記録と振り返りであり、暗号は「ユーザー自身のデータを、
ユーザー自身の端末の中で、紛失・盗難時に読まれないようにする」ためだけに使われている。
暗号化の対象はユーザー本人のデータに限られ、第三者との通信や認証には関与しない。

Apple が列挙する適用除外（56ビット以下／医療用／OS提供の暗号のみ／オープンソース／認証のみ／
HTTPS通信のみ）には素直には当てはまらない。**サードパーティのライブラリで保存時暗号化を行う**
形態は、その一覧が想定している類型と一致しないため、上記の注記4を根拠としている。

## 見直しが必要になる変更

以下のいずれかを行う場合、この判断は成り立たなくなる可能性が高いので、申告を再検討すること。

- 暗号化・復号・鍵管理を**ユーザー向けの機能として**提供する（例: バックアップファイルの
  パスフレーズ暗号化、暗号化された共有機能）
- 端末間でデータを暗号化して同期する仕組みを入れる
- 独自の暗号アルゴリズムや、標準ではない鍵導出を実装する
- 暗号強度やセキュリティを製品の主たる訴求点として販売する

## 代替案（この判断に不安がある場合）

`ITSAppUsesNonExemptEncryption` を `true` に変え、App Store Connect の輸出コンプライアンス質問に
回答したうえで、自己分類レポート（ERN の取得と BIS への年次報告）を行う経路がある。手続きは
増えるが、判断の不確実性は下がる。
